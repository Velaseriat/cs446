<%= form_for(@filefantasy) do |f| %>
  <% if @filefantasy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@filefantasy.errors.count, "error") %> prohibited this filefantasy from being saved:</h2>

      <ul>
      <% @filefantasy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
